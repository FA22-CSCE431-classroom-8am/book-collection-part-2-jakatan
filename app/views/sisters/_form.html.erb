<%= form_with(model: sister) do |form| %>
  <% if sister.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sister.errors.count, "error") %> prohibited this sister from being saved:</h2>

      <ul>
        <% sister.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.number_field :book_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
